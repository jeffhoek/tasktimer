version: 1.0
runtime: python311
run:
  pre-run:
    - pip install poetry
    - poetry config virtualenvs.create false
    - poetry install
  command: poetry run uvicorn tasktimer.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080

